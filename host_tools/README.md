# Host / Bench Tools

Python tooling for bench bring-up and testing.

- `teensyshot_panel.py` — tkinter control panel for the Teensy compressor
  controller (commanded RPM, live telemetry, dead-man stop).
- (add serial test clients / LabVIEW-adjacent helpers here)

Python 3, pyserial. Use a virtualenv (`.venv/` is gitignored).
