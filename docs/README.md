# Docs

- `rp2350_valve_node_architecture.md` — design of record for the valve/sensor node.
- `compressor_controller_handoff.md` — Teensy controller handoff/bring-up notes.
- `project_tracker.md` — live, checkable status across all subsystems (update at
  each session close).
- Datasheets / pinouts: Tiny 2350 schematic + pinout, MS5837, PCA9685, PCA9548A.

Source bundles (`*_SOURCE.md`) may live here too as reference snapshots, but the
buildable source of truth is each firmware's own folder.
